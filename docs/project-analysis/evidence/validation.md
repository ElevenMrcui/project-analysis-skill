# Skill 与模板验收记录

日期：2026-09-06。范围为本 Skill 的工具与虚构示例，不是任何真实业务项目的测试结论。

## 工具检查

执行 `python3 -m unittest discover -s project-analysis/scripts -p 'test_*.py' -v`：19 项通过。

覆盖文件遗漏、重复/悬空引用、独立清单缺失、快照变化、断链、表字段双向归属、空分母、敏感文件不读取、符号链接、多根同名文件、无效记录、图示必需性、图示断边、必需讲解、ER 连接字段归属、等数量不同 ID、分页未完成、离线渲染与嵌入 JSON 转义。

Skill 相对资源链接有效，入口少于 500 行。编辑器对分析器、示例模型、图形脚本和主题样式未报告错误。

## 浏览器检查

通过 VS Code 集成浏览器与 Playwright 对真实生成的 `file:` HTML 检查：

| 检查 | 实际结果 |
| --- | --- |
| 七类图示 | 架构、功能、细节、时序、数据流、ER、生命周期均成功返回含中文文本的 SVG |
| 离线资源 | 页面资源记录无外部加载；捕获到的页面脚本错误为 0 |
| 响应式 | 1440、1024、390、320px 下页面宽度未超过视口 |
| 数据字典 | 订单表展示 5 字段；检索 `tenants.name` 后展示租户表 2 字段；业务解释可见 |
| 功能讲解 | 用途与步骤出现在技术详情之前，术语及边界可展开查看 |
| 键盘 | Enter 打开详情、Escape 关闭、焦点返回触发按钮通过；已补嵌入式浏览器兼容处理 |
| 空结果 | 检索不存在的内容显示无匹配状态 |
| 分页导出 | 浏览器内临时扩展到 104 条，第 3 页显示 24 条；JSON 保留 104 条，CSV 包含最后一条 |
| CSV 安全 | `=FORMULA()` 类型文本导出时增加前导单引号 |
| 完整打印数据 | 打印 DOM 含最后一条临时记录及 7 个 SVG，不受分页限制 |
| 主题与动效 | 深色主题图形存在且无渲染错误；减少动效设置下动画为 none |
| 链接边界 | 原始/编码/二次编码路径穿越、绝对文件路径、不安全协议和带凭据 URL 均被拒绝 |

分页压力测试仅修改浏览器内存，重新加载后恢复原有 19 个对象。没有把临时数据写入示例 JSON。

## 限制

- 未对真实项目执行完整分析；不得据此声称实际业务功能已查全。
- 未连接运行数据库；示例结构与关系均为虚构模型。
- 未进行真实读者理解测试；通俗性目前为结构约束与 Agent 自查。
- 验证了打印数据 DOM，未实际打印到物理打印机，也未核验分页后的 PDF 排版。
- 未执行 UI UX Pro Max、impeccable 或 Archify CLI，不宣称其自动审计通过。
- 响应式和功能检查不等于完整 WCAG 审计；未完成全站色彩对比度或辅助技术认证。